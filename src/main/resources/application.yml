server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/detection_car?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: validate   # 数据库已经建好：推荐 validate
    show-sql: false

# 简单配置：管理员账号（首次启动会自动创建）
app:
  admin:
    username: admin
    password: admin123
  token:
    expireHours: 24
